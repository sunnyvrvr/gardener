<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gardener.MemberMapper">
<select id="selectById" 
	parameterType="string"
	resultType="Member" >
	SELECT * FROM member 
	WHERE loginId= #{loginId}
</select>
<insert id="signup"
	parameterType="Member">	
	INSERT INTO member(loginId, pwd, email, name, intro) 
	VALUES (#{loginId}, #{pwd}, #{email}, #{name}, #{intro})
</insert>
</mapper>