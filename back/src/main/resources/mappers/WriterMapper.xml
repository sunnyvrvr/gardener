<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gardener.WriterMapper">
<resultMap id="WriterResultMap" type="Member">
	<result property="name" column="name"/>
	<result property="intro" column="intro"/>
	<result property="profile" column="profile"/>
</resultMap>
<select id="selectByWriter"
	parameterType="int"
	resultMap="WriterResultMap">
	SELECT name, intro, profile 
	FROM member m JOIN writer w ON m.id = w.writerid 
	WHERE writerid=#{writerid}
	</select> 

<resultMap id="WriterPostResultMap" type="Post">
	<result property="mainTitle" column="mainTitle"/>
	<result property="content" column="content"/>
	<result property="mainTitleImg" column="mainTitleImg"/>
</resultMap>
<select id="selectByWriterPost"
	parameterType="int"
	resultMap="WriterPostResultMap">
	SELECT mainTitle, content, mainTitleImg
	FROM writer w
	JOIN member m ON m.id=w.writerid
	JOIN POST p ON p.userNum = m.id
	WHERE writerid = #{writerid}
</select>
<select id="subscribeWriter"
	parameterType = "string"
	resultType = "Member">
	SELECT m.name, m.profile
	FROM subscribe s
	JOIN writer w ON s.writerid = w.writerid
	JOIN member m ON w.writerid = m.id
	WHERE loginId = #{loginId}
</select>
</mapper>